# 安全策略

请不要在公开 Issue 中提交密钥、私有素材或可识别个人身份的数据。安全问题请通过仓库维护者提供的私密联系方式报告。

PaperCut Studio 默认只监听 `127.0.0.1`。若通过 `HOST=0.0.0.0` 暴露到局域网或互联网，请在前方配置访问控制和 TLS；内置服务不提供用户认证。生产环境应限制上传大小、隔离渲染进程，并仅配置可信的 ASR 命令和 ComfyUI 地址。
