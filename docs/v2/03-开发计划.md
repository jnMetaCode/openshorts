# OpenShorts · 开片 — 开发计划（v2）

节奏：每个里程碑以"一条真实成片 + 文档 + 测试"收口；发版跟着里程碑走，不按周。

## M0 · 立项与骨架（1–2 天）
- [x] 定名：OpenShorts · 开片（ADR-001）
- [x] v2 文档四份（本目录）
- [x] 主仓 README 顶部加 v2 方向说明与文档链接；个人内容拆到私有 papercut-studio，公开仓转 public
- [x] 回填器 `aoResultToProject` + 真实运行快照测试（4 条）；共享 `~/.ao`。库调用在 M1 的 `run` 接入（M0 先子进程）
- [x] 素材库 key 引导页（Pexels/Pixabay 注册链接 + 粘贴 + 探活）——探活 9-02 补齐：保存后各真发一次最小检索，✅/⛔ 当场显示
- [x] `package.json` 加依赖 `agency-orchestrator@^0.19.1`；`npx openshorts` 入口（open / sources / drama / doctor / version）
- [x] AO 短剧流水线以引用方式接入（`templates/ai-drama.template.json` + `openshorts drama`，validate/plan 已通）
- 验收：✅ `npx openshorts` 起来（/api/health ok）；✅ `openshorts drama --validate/--plan` 读到 AO 内置流水线；真出三镜留到有 key 的机器手动跑。
- TTS 音色样本已出 3 个（Xiaoxiao 女 / Yunxi 男·年轻 / Yunjian 男·沉稳），等选择。

## M1 · 口播科普，免费路径（1 周）
- [x] `templates/koubo-kepu.yaml`：话题 → 分段脚本 JSON（钩子/要点/收尾，每段 visualIntent + 英文 query + emphasis，assert 校验）→ 标题 3 选 1 + 标签 + 发布说明（含 AI 标识）
- [x] `src/sources/stock.mjs`：本地素材夹 / Pexels / Pixabay 检索、去重、缓存、license 溯源、失败降级（Pexels/Pixabay 真机待 key）
- [x] `src/voice/edge-tts.mjs`：msedge-tts 词级时间戳 + 标点回贴
- [x] 字幕样式预设 3 套；配音时长驱动镜头时长；单条 ≤ 4.5 秒
- [x] 合成到 1080×1920 mp4（AI 标识：drawtext 或元数据；响度归一 -16 LUFS）；封面图；导出 SRT；出片后自动质检（分辨率/时长/音轨/响度/字幕/封面/AI 标识/镜头）
- [x] 四步界面（输入 / 来源与花费 / 预览与调整 / 出片与发布）+ 首跑提示（文本 key、素材库 key、音色试听）；`?project=<id>` 深链；v1 编辑器移到 `/editor`
- [x] `openshorts estimate`（M1 版：免费来源计数；AI 出片接 AO preflight 在 M2）
- [x] 测试：素材匹配、裁切、字幕时轴、降级路径（202 条，三平台 CI）；真实成片《洋葱》已入 README 与官网
- 验收：全新机器 `npx openshorts` → 输入话题 → 10 分钟内拿到能发抖音的成片，全程 0 元。

## M2 · AI 短剧 + 本地生成（2 周）
- [x] 云端供应商 / 模型 / 档位 / 秒数、定妆图供应商 / 模型 = 从 AO 供应商表拉下拉（只列真机核实过的，未列可手填）
- [x] AI 短剧模板进界面（AO 子进程 + SSE；结果回填项目 JSON；本地草稿档 / 云端成片档切换；花费预览按所选输入算——顺手修了 AO `plan` 忽略 `-i` 的缺陷）
- [x] 本地生成上提到 AO：`local-sdcpp` 视频供应商（sd-cli 检测、内存分档、缺权重给下载命令、webm→mp4、preflight 不计费、doctor 就绪、串行互斥）；真机 AO 引擎 212.9 s 出片
- [x] 界面里的一键安装：sd-cli 预编译包按平台自动选 + 模型断点续传 + 许可证确认门 + 进度
- [x] 镜头级换来源重出；验收未过一键重出（AO feedback）——9-01/9-02 真跑验证：同镜三跑，只有该镜与 film 变、其余逐字节复用
- [x] 项目历史（顶栏「最近项目…」下拉 + GET /projects，按更新时间排）
- [~] 反哺进 AO：`local-sdcpp` 供应商已随 AO 0.19.2 发版（测试 + doctor 齐）；`type: stock` 素材库步骤类型未做（OpenShorts 自用 src/sources 不阻塞，AO 侧待议）
- 验收：同一条短剧，本地草稿档与云端成片档各出一版；界面能看到验收结论并重出。

## M3 · 生产力与发布（2 周）
- [x] 批量：同脚本 × 音色 × 字幕样式 × 语速（CLI `batch` / API / 界面第 4 步），真机 2 版 24 s
- [x] 发布包：抖音 / 视频号 / B 站 / Shorts 规格（标题字数、标签数、AI 标识提示），mp4+封面+SRT+文案+zip，不自动发布（草稿优先）
- [x] 英文界面（9-06：UI 全量 t()，EN 切换 / ?lang=en；官网双语 + README.en；残留：服务端状态值与引擎日志仍中文）。英文模板未做
- [x] 桌面端打包（9-07）：移植 AO electron 壳并适配；配套解掉 v1 只读阻塞（OPENSHORTS_V1_DATA 外迁写目录 + 首启播种，含回归测试）；构建期依赖拷贝时过滤（本地=CI，dmg 295→272MB）。真机验证：dev 端口顺延、本地打 arm64+x64 dmg 签名校验通过、**启动打包后的 .app 并用其内置 Node 跑通 AO 子进程**。三平台安装包 = 推 `desktop-v*` 标签（Win/Linux 首个 tag 即首测）
- 验收：一份脚本批量出 5 版；发布草稿可在平台后台看到。

## 你（产品负责人）在各里程碑要做的事
| 里程碑 | 你要做 | 预计 |
|---|---|---|
| M0 | 确认 PRD/架构；决定默认 TTS 音色候选（我给 3 个样本你选） | 30 分钟 |
| M1 | 注册 Pexels + Pixabay 免费 key；提供 3 个真实话题做验收；审第一条成片并给意见 | 1 小时 |
| M2 | 决定短剧线默认云端供应商（跟赞助谈判结果走）；点头跑本地 Q3 上限测试 | 30 分钟 |
| M3 | 准备抖音/视频号测试账号；决定英文版是否同步发 | 1 小时 |

## 分工建议
- 引擎侧改动（AO 仓库）与产品侧改动（本仓库）分开提交；AO 新能力先在 OpenShorts 里验证再上提。
- 每条真实成片都标"由 OpenShorts 实际生成 · 来源/模型"，放进 README 的成片表。
